// App-wide constants. Change values here — no need to touch component code.

// Vault auto-lock: idle time (ms) before the vault locks itself.
// 15 minutes = 15 * 60 * 1000
export const VAULT_AUTO_LOCK_MS = 2 * 60 * 1000;
